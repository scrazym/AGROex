import {StyleSheet} from 'react-native';

export const UserCabStyles = StyleSheet.create({
  userIcon: {
    width: '100%',
    height: '100%',
    borderRadius: 40,
    position: 'relative',
    zIndex: 9,
  },
});

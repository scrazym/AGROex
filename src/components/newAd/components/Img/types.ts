export type ImgProps = {
  onPress: () => void;
};

export type ImageProps = {
  id: string;
  fileName: string;
  uri: string;
  type: string;
  imageUrl: string;
};
export type ImageFromBackProps = {
  imageId: string;
  imageUrl: string;
  uri: string;
};
